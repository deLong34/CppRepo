#pragma once

namespace myLib {
	void initArray(float array[], int size);  //прототип функции инициализации массива
	void printArray(float array[], int size); //прототип функции вывода на экран массива
	void myCalc(float array[], int size);     //прототип функции подсчета + и - элементов
}